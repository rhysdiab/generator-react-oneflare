import React from 'react';
import { <%= name %>Styled } from './styled/<%= name %>Styled';

const <%= name %> = () => {
  return (
    <<%= name %>Styled>
      <%= name %>
    </<%= name %>Styled>
  );
}

export default <%= name %>;
